{
  "name": "kblog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node node_modules/ghost/index.js",
    "ship": "mkdir -p temp && buster generate --dir=temp && rsync -av --remove-source-files temp/* ./ && buster deploy --dir=. && rm -rf temp"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ghost": "^0.5.9"
  }
}
